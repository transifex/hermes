machine:
  environment:
    ALL_TESTS: 1

test:
  override:
    - python setup.py test

deployment:
  rtfd:
    branch: master
    commands:
      - curl -X POST http://readthedocs.org/build/hermes-pg-py